# Frontend Environment Variables for Development
# Vite requires VITE_ prefix for env vars to be available in the browser

# API URL - should point to your backend
VITE_API_URL=http://localhost:8000

# Environment
VITE_ENV=development

# Enable hot module replacement polling for Docker
CHOKIDAR_USEPOLLING=true
